#include <iostream>
using namespace std;
int fact(int n)
{
    if(n==0)return 1;//base case
    return fact(n-1)*n;//recursive case help to get small answer
}
int main() {
	int n;
	cin>>n;
	cout<<fact(n);
	return 0;
}
